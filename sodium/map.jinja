{% set sodium = salt['grains.filter_by']({
    'Debian': {
        'repo': 'https://github.com/jedisct1/libsodium',
        'branch': 'master',
        'rev': 'master',
    },
}, merge=salt['pillar.get']('sodium:lookup')) %}
