{% set zyre = salt['grains.filter_by']({
    'Debian': {
        'repo': 'https://github.com/zeromq/zyre',
        'branch': 'master',
        'rev': 'master',
    },
}, merge=salt['pillar.get']('zyre:lookup')) %}
