{% set go = salt['grains.filter_by']({
    'Debian': {
        'repo': 'https://github.com/golang/go',
        'branch': 'master',
        'rev': 'master',
    },
}, merge=salt['pillar.get']('go:lookup')) %}
