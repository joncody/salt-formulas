{% set zmq = salt['grains.filter_by']({
    'Debian': {
        'repo': 'https://github.com/zeromq/libzmq',
        'branch': 'master',
        'rev': 'master',
    },
}, merge=salt['pillar.get']('zmq:lookup')) %}
